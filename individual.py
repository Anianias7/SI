import random

# def __init__(self, number_od_genes):
#     self.number_of_genes = number_od_genes
#     self.list_of_genes = list(range(self.number_of_genes))
# def create_individual(self):
#     genes = self.list_of_genes.copy()
#     random.shuffle(genes)
#     return genes
